@Library('jenkins-shared-libraries@gelo') _

pipeline {
    
    agent {
      kubernetes { yaml pipelineGetAgent(devopscli_version: 'latest') }
    }

    stages{
        stage('Test Container') {
            steps {
                container('devopscli') {
                    sh "devopscli assume prod"
                    sh "aws sts get-caller-identity"
                }
            }
        }
    }
}